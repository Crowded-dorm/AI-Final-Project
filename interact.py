import matplotlib.pyplot as plt

def selectSourceImage(img_arr: list):
    """
    """
    # Show all images (plt)
    id = input('Enter the source image id: ')
    # Return the image with the id
    pass
